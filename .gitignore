# --- Python / backend ---
backend/venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Local database (holds personal notes; prod will use a cloud DB) ---
# Kept out of the repo so notes aren't exposed to anyone who can see it.
*.db
*.db-journal
*.sqlite
*.sqlite3
note_tracker.db
note_tracker.backup.db

# Environment / secrets
.env
.env.*
!.env.example

# --- OS / editor ---
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

backend/*.db
backend/*.sqlite3

